import { Request, Response } from "express"

export const getProductsController = (req: Request, res: Response) => {
    try {
        
    } catch (error) {
        
    }
}

export const createProductsController = (req: Request, res: Response) => {
    try {
        
    } catch (error) {
        
    }
}